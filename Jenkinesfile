pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building demo app'
            }
        }

        stage('Test') {
            steps {
                sh 'cat app.txt'
            }
        }

    }
}
